= spinner
span.install
    = link_to(t("install"), apps_engine.install_path(:id => app.identifier), :method => :post, :remote => true, :class => 'install-app-in-background btnn btn btn-info btn-sm ')
    = link_to(t("Docker Install"), apps_engine.install_path(:id => app.identifier, :docker => true), :method => :post, :remote => true, :class => 'install-app-in-background btnn btn btn-info btn-sm ')